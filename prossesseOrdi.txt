En avance, voire le code DeChoix


procedure prossesseOrdi;
var
i, j, t, w, x : integer ;
score : integer ;
couleurFaible : string ;
begin
  verification ; // return une liste de carte qui sont jouable avec une variable de nombre de carte "veri"
  mémoire ; // return les mémoire qu'il a fait dans els passage précédant : appèle au point faible de joueur Humain.
  // une liste de deck qui se crée ?

  if veri = 0 then
  begin
  tirerUCarte ;
  PassTours ;
  end;


  if veri > 0 then
  begin
    deChoix;
    if deChoix = 0 then
    begin
      
    end
    elif deChoix = 1 then
    begin
      couleurFaible := memoireCarte ;
      for i := 0 to veri -1 do
      begin
      score = 0 ;
      if (targetColor > 0) and ((v = -3) or (v = -4) or (c = targetColor)) then
      begin
        score := score + 11; // 가장 높은 점수 부여
      end;
      if v = -4 then score := score + 10
      else if v = -2 then score := score + 9
      else if v = -5 then score := score + 8
      else if v = -1 then score := score + 7
      else if (v >= 1) and (v <= 9) then score := score + 3 + v * 0.1
      else if v = 0 then score := score + 2
      else if v = -3 then score := score + 1
      end:  
    end;
  end;




procedure verification;
var

begin

end;



